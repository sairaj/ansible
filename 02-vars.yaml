- name: Demo on using variables
  hosts: all
  vars:
    URL: play.google.com
  vars_files:
    - demo-vars.yaml
  vars_prompt:
    - name: URL
      prompt: Enter URL value
      private: false

  tasks:
    - name: Printing URL
      ansible.builtin.debug:
        msg: "{{ URL }}"